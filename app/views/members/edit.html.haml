= page_title t('update', model: Member.model_name.human)

%nav
  %ol.breadcrumb
    %li.breadcrumb-item
      = link_to Member.model_name.human(count: 2), members_path
    %li.breadcrumb-item
      = link_to @member.id, members_path(@member)
    %li.breadcrumb-item.active
      = t('breadcrumb_edit')

= render 'form'
